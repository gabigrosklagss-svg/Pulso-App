import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';
test('interface contém navegação móvel, preferência e textos normativos',async()=>{const html=await readFile('public/index.html','utf8');const css=await readFile('public/styles.css','utf8');const js=await readFile('public/app.js','utf8');for(const label of ['Buscar','Procuras','Conversas','Perfil'])assert.match(html,new RegExp(label));assert.match(html,/Olá! O que você está procurando\?/);assert.match(html,/Somente o resumo confirmado/);assert.match(css,/min-width:320px/);assert.match(css,/prefers-reduced-motion/);assert.match(js,/localStorage\.setItem\('pulso:theme'/);});
